import joi from "joi";

export const createGroup = joi.object({});

export const getMyGroups = joi.object({});

export const getGroupById = joi.object({});

export const updateGroup = joi.object({});

export const deleteGroup = joi.object({});

export const addMember = joi.object({});

export const removeMember = joi.object({});

export const leaveGroup = joi.object({});