import joi from "joi";

export const sendMessage = joi.object({});
export const getMessages = joi.object({});
export const getMessageById = joi.object({});
export const deleteMessage = joi.object({});
export const updateMessage = joi.object({});
